<!DOCTYPE html>
<html>
  <head>
    <title>A Little Love Story</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag asset_path("heart.svg") %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="https://unpkg.com/trix@2.0.8/dist/trix.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="https://kit.fontawesome.com/b8199f9912.js" crossorigin="anonymous"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="dark:bg-gray-50 dark:text-gray-700 flex flex-col h-screen pb-4">   
      <%= render "shared/header" %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <main class="flex-grow">
        <%= yield %>
      </main>
  </body>
</html>
